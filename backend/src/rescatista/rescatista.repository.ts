/*
import { Rescatista } from "../entities/rescatista.entity.js";

export interface RescatistaRepository {
    findAllRescatista(): Promise<Rescatista[]>;
    getOneRescatista(numero: number): Promise<Rescatista | null>;
    createRescatista(rescatista: Rescatista): Promise<Rescatista>;
    updateRescatista(rescatista: Rescatista): Promise<Rescatista | null>;
    deleteRescatista(rescatista: Rescatista): Promise<void>;
    findOneByDNI(dni: string): Promise<Rescatista | null>;
}
*/